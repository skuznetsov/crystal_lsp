Update: state cleanup and status
- Removed local files: .claude/settings.local.json and the built binary show_diagnostics (now only parser.cr is dirty).
- Re-tested minimal cases:
  - /tmp/test_void.cr (size : LibC::SizeT, return : Void*) → still 1 diag (unexpected Colon @4:39).
  - /tmp/test_self.cr (same param, no return type) → 0 diags.
  - boehm_min (same pattern) → 4 diags.
- Command-call tweaks didn’t fix named args; return-type parsing likely still leaking state. Eval times out, so using ./show_diagnostics binary before removal.
- Next: enforce boundary after return type / reset pointer-suffix state; ensure method body starts at Identifier (Crystal) not Symbol(:gc).
