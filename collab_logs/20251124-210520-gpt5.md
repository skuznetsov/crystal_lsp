Checkpoint after fix:
- Return-type state leak fixed; `Crystal.trace ... size: size` in typed methods now parses (test_void/boehm_min zero diagnostics via `crystal run tools/show_diagnostics.cr`).
- Changed: return type parse resets @allow_pointer_suffix and resets both @allow_pointer_suffix/@no_type_declaration + skip_statement_end before body.
- Full stdlib scan still timing out (WATCHDOG=1, SCAN_TIMEOUT=60, 120s cap). Need to rerun with higher timeout or chunked paths.
- Repo dirty: parser.cr + collab_logs (no commits).
